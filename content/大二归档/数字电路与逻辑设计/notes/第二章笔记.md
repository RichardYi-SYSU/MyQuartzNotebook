## 奇偶校验码

- 奇校验就是要求原码+校验码之后1的个数为奇数
- 偶校验就是要求原码+校验码之后1的个数为偶数

## 循环冗余校验码

- 数据码和生成码
- 第一步是在数据码后补上生成码位数-1的0
- 然后让数据码给生成码做模2除法，这里不用考虑借位直接减$0-1=1,1-0=1,0-0=0,1-0=1$
- 得到的余数如果为0，那就直接发送数据位和生成位
- 余数不为0，比如110100110000（数据码）和1010（生成码）得到余数为100，那么发送的数据码就变为110100110100
- 